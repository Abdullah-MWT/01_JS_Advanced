// ********************************** OPERATIONS COMPARISON ****************************************

// Some simple Operations COmparison =>

console.log(2 >= -5);
console.log(23 > 6);

// Some Advanced Operations Comparison

console.log("2" == 2)  // true
// The above code will give output true, because It,ll just change the first string into number and then compare them, which are equal


console.log("2" === 2);
// The above code will give output false, bbecause it doesn,t change type of data but it compares their values + Type as well

console.log(null > 0);    // false
console.log(null == 0);    //false
console.log(null >= 0);    //true
//The last is true , because due to comaparison operators, it,ll first convert the null type to num, i.e 0, and it,s equal then 
